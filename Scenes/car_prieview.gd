extends Node3D

# Hårdkodade mesh-namn
const MESH_NAMES = ["BlackCar", "BrownPickup", "GreenCar", "RedCar"]

#func set_visible_mesh(index: int) -> void:
	#for i in MESH_NAMES.size():
		#var mesh_node = get_node(MESH_NAMES[i])
		#mesh_node.visible = (i == index)
